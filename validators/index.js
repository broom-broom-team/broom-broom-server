const auth = require("../validators/auth.valid");
const address = require("../validators/address.valid");
const user = require("../validators/user.valid");
const post = require("../validators/post.valid");

module.exports = { auth, address, user, post };
